# Feature X 🗑️ — redundant / drop candidate
#
# /sas-url/ (§4 "can be deleted"). SAS rewrite lives in /api/ (feature 4).
# Characterised before deletion; see docs/QUIRKS.md B2.

location /sas-url/ {
  add_header Content-Disposition: inline;
  proxy_pass https://${SAS_URL_DOMAIN_NAME}/;
}
