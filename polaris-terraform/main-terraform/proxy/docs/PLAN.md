# PLAN — proxy refactor roadmap

| Phase                     | Item                                                                                               | Status | Notes                                                                                                                                   |
| ------------------------- | -------------------------------------------------------------------------------------------------- | :----: | --------------------------------------------------------------------------------------------------------------------------------------- |
| **1 — Harness + slice**   | Integration and unit tests                                                                         |   ✅   | Golden-master harness built and green on live **and** `--next` (see [`../README.md`](../README.md)).                                    |
| 1                         | Reorganise into separate files                                                                     |   ✅   | Live `nginx.conf` sliced into `config/features/<name>/<name>.{conf,js}`; parent is preamble + `include`s.                               |
| 1                         | Small prune of redundant locations                                                                 |   ✅   | `/sas-url/` dropped from the `next` config; further drop-candidates recorded in QUIRKS (D8, D10).                                       |
| **2 — Refactor sections** | Refactor separate sections (njs split + consolidation)                                             |   🟡   | Per-feature njs modules + shared `common/cms-detection.js` done. Remaining: `replaceCmsDomains` delete/repair decision (QUIRKS **B1**). |
| 2                         | Cutover — swap `nginx-next.conf.template` → `nginx.conf.template`                                  |   ⬜   | Deploy plumbing already parked/inert ([`PROXY.md` §6.5](./PROXY.md)); one-blob swap, tests have proved parity. Do in staging first.     |
| **3 — Drop polaris-ddei** | Non-DDEI init behind `NON_DDEI_INIT_ENABLED` — njs reimpl of DDEI `/api/init/`                      |   ✅   | Feature `polaris-non-ddei`; switch at `/auth-refresh-inbound` (`if` + `rewrite … last`), off = today. Golden master green both sides. Terraform app-setting **not** added — set it **manually in deployed QA** to test. |
| 3                         | Drop the `polaris-ddei` routes + project                                                           |   ⬜   | After the non-ddei path is proven in QA (and cutover). Self-contained / independently deletable (QUIRKS **A4**, **D7**).                |
| **4 — Auth handover**     | Rationalise the auth-handover flows (incl. CGI case-specific → `/go`)                              |   ⬜   | Umbrella. The sub-items below collectively unlock **collapsing `/auth-refresh-inbound` into `/init`** — removing the endpoint and its second 302 hop. |
| 4                         | `/materials` → drop explicit `auth-refresh-inbound`, pass `polaris-ui-url` bare                    |   ⬜   | Lean on `_argsShim`'s no-`r` branch; whitelist already has the relative entry. Behavioural delta (folds in `referer`) → before/after golden master. |
| 4                         | Audit + fix the UI's `REACT_APP_REAUTH_REDIRECT_URL_INBOUND`                                       |   ⬜   | Cross-repo: the UI is the other external namer of the string (every `.env`).                                                           |
| 4                         | Validate `polaris-ui-url` in the njs init (open-redirect guard)                                    |   ⬜   | `polaris-non-ddei` currently 302s to it **unchecked**; the whitelist's role moves from gating `r` to the landing URL.                   |
| **5 — v2 auth + MDS**     | Add v2 auth into the flow in test                                                                  |   ⬜   | v2 auth spike currently lives in the **global-components** repo, not Polaris.                                                           |
| 5                         | Get MDS endpoint built                                                                             |   ⬜   |                                                                                                                                         |
| **Backlog**               | Feed global-components logic back to repo                                                          |   ⬜   |                                                                                                                                         |
| Backlog                   | Remove `SAS_URL_DOMAIN_NAME` from terraform variables                                              |   ⬜   | Follows the `/sas-url/` prune (Phase 1).                                                                                                |

# New MDS endpoint
